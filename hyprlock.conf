background {
    monitor =
    path = screenshot
    blur_passes = 2
    blur_size = 7
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    font_size = 64
    position = 0, 80
    halign = center
    valign = center
}

input-field {
    monitor =
    size = 300, 50
    position = 0, -40
    halign = center
    valign = center
}
